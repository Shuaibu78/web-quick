import styled from "styled-components";

interface IInputProps {
  buttonColor?: string;
  valueColor?: string;
  paddingSize?: string;
  borderColor?: string;
  borderSize?: string;
  borderRadius?: string;
  fontSize?: string;
  width?: string;
}

export const LineChartContainer = styled.div`
  width: 100%;
  height: 100%;
`;
