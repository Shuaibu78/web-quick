export interface IBanks {
  bankCode: string;
  bankName: string;
};

export interface IBankDetails {
  accountName: string;
  accountNumber: string;
  bankDetailId: string;
  bankName: string;
  shopId: string;
}
